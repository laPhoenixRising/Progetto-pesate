<!DOCTYPE html>
<html>
  <head>
    <title>Pesate</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= yield %>
    <%= link_to("English", "/language/en", data: { turbo_method: :put })%>
    <%= link_to("Italiano", "/language/it", data: { turbo_method: :put })%>
    <% if session[:utente] %> 
      <%= link_to(t("log_out"), login_path, data: { turbo_method: :delete }) %> 
    <% end %>
  </body>
</html>
