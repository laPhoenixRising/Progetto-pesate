<h1>Archivio pesate</h1>
<ul>
<% @weighings.each do |weighing| %>
  <li>
    <%= weighing.value %>
    <%= link_to("Delete", weighing_path(weighing.id), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }) %>
    <%= link_to("Edit", edit_weighing_path(weighing.id)) %>
  </li>
<% end %>
</ul>
<%== pagy_nav(@pagy) if @pagy.pages > 1 %>

<%= link_to("homepage", root_path) %>  

